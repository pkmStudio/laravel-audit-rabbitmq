{
    "name": "pkmstudio/audit",
    "description": "Shared audit package: owen-it Audit model (readable unicode + nested JSON decode), source-tagging trait, outbox producer over rabbit-transport, and app-agnostic inbox consumer.",
    "type": "library",
    "license": "proprietary",
    "repositories": [
        {
            "type": "vcs",
            "url": "https://github.com/pkmStudio/laravel-rabbitmq-transport",
            "no-api": true
        }
    ],
    "require": {
        "php": "^8.4",
        "illuminate/support": "^12.0",
        "illuminate/database": "^12.0",
        "illuminate/console": "^12.0",
        "illuminate/contracts": "^12.0",
        "owen-it/laravel-auditing": "^14.0",
        "pkmstudio/rabbit-transport": "dev-master"
    },
    "require-dev": {
        "orchestra/testbench": "^10.0",
        "pestphp/pest": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "PkmStudio\\Audit\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "PkmStudio\\Audit\\Tests\\": "tests/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "PkmStudio\\Audit\\AuditServiceProvider"
            ]
        }
    },
    "config": {
        "allow-plugins": {
            "pestphp/pest-plugin": true,
            "php-http/discovery": true
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
